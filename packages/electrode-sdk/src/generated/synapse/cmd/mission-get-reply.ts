// automatically generated by the FlatBuffers compiler, do not modify

import * as flatbuffers from 'flatbuffers';

import { MissionItemData } from '../../synapse/cmd/mission-item-data.js';
import { CommandResultCode } from '../../synapse/types/command-result-code.js';


export class MissionGetReply {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):MissionGetReply {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsMissionGetReply(bb:flatbuffers.ByteBuffer, obj?:MissionGetReply):MissionGetReply {
  return (obj || new MissionGetReply()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsMissionGetReply(bb:flatbuffers.ByteBuffer, obj?:MissionGetReply):MissionGetReply {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new MissionGetReply()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

result():CommandResultCode {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : CommandResultCode.Accepted;
}

missionId():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

planVersion():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

offset():number {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

total():number {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

resultDetail():number {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.readInt32(this.bb_pos + offset) : 0;
}

failedSeq():number {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

flags():number {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? this.bb!.readUint16(this.bb_pos + offset) : 0;
}

items(index: number, obj?:MissionItemData):MissionItemData|null {
  const offset = this.bb!.__offset(this.bb_pos, 20);
  return offset ? (obj || new MissionItemData()).__init(this.bb!.__vector(this.bb_pos + offset) + index * 32, this.bb!) : null;
}

itemsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 20);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

static startMissionGetReply(builder:flatbuffers.Builder) {
  builder.startObject(9);
}

static addResult(builder:flatbuffers.Builder, result:CommandResultCode) {
  builder.addFieldInt8(0, result, CommandResultCode.Accepted);
}

static addMissionId(builder:flatbuffers.Builder, missionId:number) {
  builder.addFieldInt32(1, missionId, 0);
}

static addPlanVersion(builder:flatbuffers.Builder, planVersion:number) {
  builder.addFieldInt32(2, planVersion, 0);
}

static addOffset(builder:flatbuffers.Builder, offset:number) {
  builder.addFieldInt32(3, offset, 0);
}

static addTotal(builder:flatbuffers.Builder, total:number) {
  builder.addFieldInt32(4, total, 0);
}

static addResultDetail(builder:flatbuffers.Builder, resultDetail:number) {
  builder.addFieldInt32(5, resultDetail, 0);
}

static addFailedSeq(builder:flatbuffers.Builder, failedSeq:number) {
  builder.addFieldInt32(6, failedSeq, 0);
}

static addFlags(builder:flatbuffers.Builder, flags:number) {
  builder.addFieldInt16(7, flags, 0);
}

static addItems(builder:flatbuffers.Builder, itemsOffset:flatbuffers.Offset) {
  builder.addFieldOffset(8, itemsOffset, 0);
}

static startItemsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(32, numElems, 4);
}

static endMissionGetReply(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createMissionGetReply(builder:flatbuffers.Builder, result:CommandResultCode, missionId:number, planVersion:number, offset:number, total:number, resultDetail:number, failedSeq:number, flags:number, itemsOffset:flatbuffers.Offset):flatbuffers.Offset {
  MissionGetReply.startMissionGetReply(builder);
  MissionGetReply.addResult(builder, result);
  MissionGetReply.addMissionId(builder, missionId);
  MissionGetReply.addPlanVersion(builder, planVersion);
  MissionGetReply.addOffset(builder, offset);
  MissionGetReply.addTotal(builder, total);
  MissionGetReply.addResultDetail(builder, resultDetail);
  MissionGetReply.addFailedSeq(builder, failedSeq);
  MissionGetReply.addFlags(builder, flags);
  MissionGetReply.addItems(builder, itemsOffset);
  return MissionGetReply.endMissionGetReply(builder);
}
}
