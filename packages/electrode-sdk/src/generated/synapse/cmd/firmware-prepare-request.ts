// automatically generated by the FlatBuffers compiler, do not modify

import * as flatbuffers from 'flatbuffers';



export class FirmwarePrepareRequest {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):FirmwarePrepareRequest {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsFirmwarePrepareRequest(bb:flatbuffers.ByteBuffer, obj?:FirmwarePrepareRequest):FirmwarePrepareRequest {
  return (obj || new FirmwarePrepareRequest()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsFirmwarePrepareRequest(bb:flatbuffers.ByteBuffer, obj?:FirmwarePrepareRequest):FirmwarePrepareRequest {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new FirmwarePrepareRequest()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

updateId():string|null
updateId(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
updateId(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

target():string|null
target(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
target(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

boardId():string|null
boardId(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
boardId(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

version():string|null
version(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
version(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

imageSize():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt('0');
}

imageSha256():string|null
imageSha256(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
imageSha256(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

selectiveSha256():string|null
selectiveSha256(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
selectiveSha256(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

requestedChunkSize():number {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

chunkCount():number {
  const offset = this.bb!.__offset(this.bb_pos, 20);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

signature(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 22);
  return offset ? this.bb!.readUint8(this.bb!.__vector(this.bb_pos + offset) + index) : 0;
}

signatureLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 22);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

signatureArray():Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 22);
  return offset ? new Uint8Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

manifest():string|null
manifest(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
manifest(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 24);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

static startFirmwarePrepareRequest(builder:flatbuffers.Builder) {
  builder.startObject(11);
}

static addUpdateId(builder:flatbuffers.Builder, updateIdOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, updateIdOffset, 0);
}

static addTarget(builder:flatbuffers.Builder, targetOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, targetOffset, 0);
}

static addBoardId(builder:flatbuffers.Builder, boardIdOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, boardIdOffset, 0);
}

static addVersion(builder:flatbuffers.Builder, versionOffset:flatbuffers.Offset) {
  builder.addFieldOffset(3, versionOffset, 0);
}

static addImageSize(builder:flatbuffers.Builder, imageSize:bigint) {
  builder.addFieldInt64(4, imageSize, BigInt('0'));
}

static addImageSha256(builder:flatbuffers.Builder, imageSha256Offset:flatbuffers.Offset) {
  builder.addFieldOffset(5, imageSha256Offset, 0);
}

static addSelectiveSha256(builder:flatbuffers.Builder, selectiveSha256Offset:flatbuffers.Offset) {
  builder.addFieldOffset(6, selectiveSha256Offset, 0);
}

static addRequestedChunkSize(builder:flatbuffers.Builder, requestedChunkSize:number) {
  builder.addFieldInt32(7, requestedChunkSize, 0);
}

static addChunkCount(builder:flatbuffers.Builder, chunkCount:number) {
  builder.addFieldInt32(8, chunkCount, 0);
}

static addSignature(builder:flatbuffers.Builder, signatureOffset:flatbuffers.Offset) {
  builder.addFieldOffset(9, signatureOffset, 0);
}

static createSignatureVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset {
  builder.startVector(1, data.length, 1);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt8(data[i]!);
  }
  return builder.endVector();
}

static startSignatureVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(1, numElems, 1);
}

static addManifest(builder:flatbuffers.Builder, manifestOffset:flatbuffers.Offset) {
  builder.addFieldOffset(10, manifestOffset, 0);
}

static endFirmwarePrepareRequest(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createFirmwarePrepareRequest(builder:flatbuffers.Builder, updateIdOffset:flatbuffers.Offset, targetOffset:flatbuffers.Offset, boardIdOffset:flatbuffers.Offset, versionOffset:flatbuffers.Offset, imageSize:bigint, imageSha256Offset:flatbuffers.Offset, selectiveSha256Offset:flatbuffers.Offset, requestedChunkSize:number, chunkCount:number, signatureOffset:flatbuffers.Offset, manifestOffset:flatbuffers.Offset):flatbuffers.Offset {
  FirmwarePrepareRequest.startFirmwarePrepareRequest(builder);
  FirmwarePrepareRequest.addUpdateId(builder, updateIdOffset);
  FirmwarePrepareRequest.addTarget(builder, targetOffset);
  FirmwarePrepareRequest.addBoardId(builder, boardIdOffset);
  FirmwarePrepareRequest.addVersion(builder, versionOffset);
  FirmwarePrepareRequest.addImageSize(builder, imageSize);
  FirmwarePrepareRequest.addImageSha256(builder, imageSha256Offset);
  FirmwarePrepareRequest.addSelectiveSha256(builder, selectiveSha256Offset);
  FirmwarePrepareRequest.addRequestedChunkSize(builder, requestedChunkSize);
  FirmwarePrepareRequest.addChunkCount(builder, chunkCount);
  FirmwarePrepareRequest.addSignature(builder, signatureOffset);
  FirmwarePrepareRequest.addManifest(builder, manifestOffset);
  return FirmwarePrepareRequest.endFirmwarePrepareRequest(builder);
}

unpack(): FirmwarePrepareRequestT {
  return new FirmwarePrepareRequestT(
    this.updateId(),
    this.target(),
    this.boardId(),
    this.version(),
    this.imageSize(),
    this.imageSha256(),
    this.selectiveSha256(),
    this.requestedChunkSize(),
    this.chunkCount(),
    this.bb!.createScalarList(this.signature.bind(this), this.signatureLength()),
    this.manifest()
  );
}


unpackTo(_o: FirmwarePrepareRequestT): void {
  _o.updateId = this.updateId();
  _o.target = this.target();
  _o.boardId = this.boardId();
  _o.version = this.version();
  _o.imageSize = this.imageSize();
  _o.imageSha256 = this.imageSha256();
  _o.selectiveSha256 = this.selectiveSha256();
  _o.requestedChunkSize = this.requestedChunkSize();
  _o.chunkCount = this.chunkCount();
  _o.signature = this.bb!.createScalarList(this.signature.bind(this), this.signatureLength());
  _o.manifest = this.manifest();
}
}

export class FirmwarePrepareRequestT {
constructor(
  public updateId: string|Uint8Array|null = null,
  public target: string|Uint8Array|null = null,
  public boardId: string|Uint8Array|null = null,
  public version: string|Uint8Array|null = null,
  public imageSize: bigint = BigInt('0'),
  public imageSha256: string|Uint8Array|null = null,
  public selectiveSha256: string|Uint8Array|null = null,
  public requestedChunkSize: number = 0,
  public chunkCount: number = 0,
  public signature: (number)[] = [],
  public manifest: string|Uint8Array|null = null
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const updateId = (this.updateId !== null ? builder.createString(this.updateId!) : 0);
  const target = (this.target !== null ? builder.createString(this.target!) : 0);
  const boardId = (this.boardId !== null ? builder.createString(this.boardId!) : 0);
  const version = (this.version !== null ? builder.createString(this.version!) : 0);
  const imageSha256 = (this.imageSha256 !== null ? builder.createString(this.imageSha256!) : 0);
  const selectiveSha256 = (this.selectiveSha256 !== null ? builder.createString(this.selectiveSha256!) : 0);
  const signature = FirmwarePrepareRequest.createSignatureVector(builder, this.signature);
  const manifest = (this.manifest !== null ? builder.createString(this.manifest!) : 0);

  return FirmwarePrepareRequest.createFirmwarePrepareRequest(builder,
    updateId,
    target,
    boardId,
    version,
    this.imageSize,
    imageSha256,
    selectiveSha256,
    this.requestedChunkSize,
    this.chunkCount,
    signature,
    manifest
  );
}
}
