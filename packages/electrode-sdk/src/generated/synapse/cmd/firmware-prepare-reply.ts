// automatically generated by the FlatBuffers compiler, do not modify

import * as flatbuffers from 'flatbuffers';

import { FirmwareUpdateState } from '../../synapse/cmd/firmware-update-state';
import { CommandResultCode } from '../../synapse/types/command-result-code';


export class FirmwarePrepareReply {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):FirmwarePrepareReply {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsFirmwarePrepareReply(bb:flatbuffers.ByteBuffer, obj?:FirmwarePrepareReply):FirmwarePrepareReply {
  return (obj || new FirmwarePrepareReply()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsFirmwarePrepareReply(bb:flatbuffers.ByteBuffer, obj?:FirmwarePrepareReply):FirmwarePrepareReply {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new FirmwarePrepareReply()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

result():CommandResultCode {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : CommandResultCode.Accepted;
}

updateId():string|null
updateId(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
updateId(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

state():FirmwareUpdateState {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : FirmwareUpdateState.Unknown;
}

acceptedChunkSize():number {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

message():string|null
message(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
message(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

resultDetail():number {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.readInt32(this.bb_pos + offset) : 0;
}

static startFirmwarePrepareReply(builder:flatbuffers.Builder) {
  builder.startObject(6);
}

static addResult(builder:flatbuffers.Builder, result:CommandResultCode) {
  builder.addFieldInt8(0, result, CommandResultCode.Accepted);
}

static addUpdateId(builder:flatbuffers.Builder, updateIdOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, updateIdOffset, 0);
}

static addState(builder:flatbuffers.Builder, state:FirmwareUpdateState) {
  builder.addFieldInt8(2, state, FirmwareUpdateState.Unknown);
}

static addAcceptedChunkSize(builder:flatbuffers.Builder, acceptedChunkSize:number) {
  builder.addFieldInt32(3, acceptedChunkSize, 0);
}

static addMessage(builder:flatbuffers.Builder, messageOffset:flatbuffers.Offset) {
  builder.addFieldOffset(4, messageOffset, 0);
}

static addResultDetail(builder:flatbuffers.Builder, resultDetail:number) {
  builder.addFieldInt32(5, resultDetail, 0);
}

static endFirmwarePrepareReply(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createFirmwarePrepareReply(builder:flatbuffers.Builder, result:CommandResultCode, updateIdOffset:flatbuffers.Offset, state:FirmwareUpdateState, acceptedChunkSize:number, messageOffset:flatbuffers.Offset, resultDetail:number):flatbuffers.Offset {
  FirmwarePrepareReply.startFirmwarePrepareReply(builder);
  FirmwarePrepareReply.addResult(builder, result);
  FirmwarePrepareReply.addUpdateId(builder, updateIdOffset);
  FirmwarePrepareReply.addState(builder, state);
  FirmwarePrepareReply.addAcceptedChunkSize(builder, acceptedChunkSize);
  FirmwarePrepareReply.addMessage(builder, messageOffset);
  FirmwarePrepareReply.addResultDetail(builder, resultDetail);
  return FirmwarePrepareReply.endFirmwarePrepareReply(builder);
}

unpack(): FirmwarePrepareReplyT {
  return new FirmwarePrepareReplyT(
    this.result(),
    this.updateId(),
    this.state(),
    this.acceptedChunkSize(),
    this.message(),
    this.resultDetail()
  );
}


unpackTo(_o: FirmwarePrepareReplyT): void {
  _o.result = this.result();
  _o.updateId = this.updateId();
  _o.state = this.state();
  _o.acceptedChunkSize = this.acceptedChunkSize();
  _o.message = this.message();
  _o.resultDetail = this.resultDetail();
}
}

export class FirmwarePrepareReplyT {
constructor(
  public result: CommandResultCode = CommandResultCode.Accepted,
  public updateId: string|Uint8Array|null = null,
  public state: FirmwareUpdateState = FirmwareUpdateState.Unknown,
  public acceptedChunkSize: number = 0,
  public message: string|Uint8Array|null = null,
  public resultDetail: number = 0
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const updateId = (this.updateId !== null ? builder.createString(this.updateId!) : 0);
  const message = (this.message !== null ? builder.createString(this.message!) : 0);

  return FirmwarePrepareReply.createFirmwarePrepareReply(builder,
    this.result,
    updateId,
    this.state,
    this.acceptedChunkSize,
    message,
    this.resultDetail
  );
}
}
