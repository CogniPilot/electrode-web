// automatically generated by the FlatBuffers compiler, do not modify

import * as flatbuffers from 'flatbuffers';

import { FirmwareUpdateState } from '../../synapse/cmd/firmware-update-state';
import { CommandResultCode } from '../../synapse/types/command-result-code';


export class FirmwareCommitReply {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):FirmwareCommitReply {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsFirmwareCommitReply(bb:flatbuffers.ByteBuffer, obj?:FirmwareCommitReply):FirmwareCommitReply {
  return (obj || new FirmwareCommitReply()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsFirmwareCommitReply(bb:flatbuffers.ByteBuffer, obj?:FirmwareCommitReply):FirmwareCommitReply {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new FirmwareCommitReply()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

result():CommandResultCode {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : CommandResultCode.Accepted;
}

updateId():string|null
updateId(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
updateId(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

state():FirmwareUpdateState {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : FirmwareUpdateState.Unknown;
}

rebootRequired():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : false;
}

message():string|null
message(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
message(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

resultDetail():number {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.readInt32(this.bb_pos + offset) : 0;
}

static startFirmwareCommitReply(builder:flatbuffers.Builder) {
  builder.startObject(6);
}

static addResult(builder:flatbuffers.Builder, result:CommandResultCode) {
  builder.addFieldInt8(0, result, CommandResultCode.Accepted);
}

static addUpdateId(builder:flatbuffers.Builder, updateIdOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, updateIdOffset, 0);
}

static addState(builder:flatbuffers.Builder, state:FirmwareUpdateState) {
  builder.addFieldInt8(2, state, FirmwareUpdateState.Unknown);
}

static addRebootRequired(builder:flatbuffers.Builder, rebootRequired:boolean) {
  builder.addFieldInt8(3, +rebootRequired, +false);
}

static addMessage(builder:flatbuffers.Builder, messageOffset:flatbuffers.Offset) {
  builder.addFieldOffset(4, messageOffset, 0);
}

static addResultDetail(builder:flatbuffers.Builder, resultDetail:number) {
  builder.addFieldInt32(5, resultDetail, 0);
}

static endFirmwareCommitReply(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createFirmwareCommitReply(builder:flatbuffers.Builder, result:CommandResultCode, updateIdOffset:flatbuffers.Offset, state:FirmwareUpdateState, rebootRequired:boolean, messageOffset:flatbuffers.Offset, resultDetail:number):flatbuffers.Offset {
  FirmwareCommitReply.startFirmwareCommitReply(builder);
  FirmwareCommitReply.addResult(builder, result);
  FirmwareCommitReply.addUpdateId(builder, updateIdOffset);
  FirmwareCommitReply.addState(builder, state);
  FirmwareCommitReply.addRebootRequired(builder, rebootRequired);
  FirmwareCommitReply.addMessage(builder, messageOffset);
  FirmwareCommitReply.addResultDetail(builder, resultDetail);
  return FirmwareCommitReply.endFirmwareCommitReply(builder);
}

unpack(): FirmwareCommitReplyT {
  return new FirmwareCommitReplyT(
    this.result(),
    this.updateId(),
    this.state(),
    this.rebootRequired(),
    this.message(),
    this.resultDetail()
  );
}


unpackTo(_o: FirmwareCommitReplyT): void {
  _o.result = this.result();
  _o.updateId = this.updateId();
  _o.state = this.state();
  _o.rebootRequired = this.rebootRequired();
  _o.message = this.message();
  _o.resultDetail = this.resultDetail();
}
}

export class FirmwareCommitReplyT {
constructor(
  public result: CommandResultCode = CommandResultCode.Accepted,
  public updateId: string|Uint8Array|null = null,
  public state: FirmwareUpdateState = FirmwareUpdateState.Unknown,
  public rebootRequired: boolean = false,
  public message: string|Uint8Array|null = null,
  public resultDetail: number = 0
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const updateId = (this.updateId !== null ? builder.createString(this.updateId!) : 0);
  const message = (this.message !== null ? builder.createString(this.message!) : 0);

  return FirmwareCommitReply.createFirmwareCommitReply(builder,
    this.result,
    updateId,
    this.state,
    this.rebootRequired,
    message,
    this.resultDetail
  );
}
}
