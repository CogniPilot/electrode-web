// automatically generated by the FlatBuffers compiler, do not modify

import * as flatbuffers from 'flatbuffers';

import { FirmwareUpdateState } from '../../synapse/cmd/firmware-update-state';
import { CommandResultCode } from '../../synapse/types/command-result-code';


export class FirmwareStatusReply {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):FirmwareStatusReply {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsFirmwareStatusReply(bb:flatbuffers.ByteBuffer, obj?:FirmwareStatusReply):FirmwareStatusReply {
  return (obj || new FirmwareStatusReply()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsFirmwareStatusReply(bb:flatbuffers.ByteBuffer, obj?:FirmwareStatusReply):FirmwareStatusReply {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new FirmwareStatusReply()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

result():CommandResultCode {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : CommandResultCode.Accepted;
}

updateId():string|null
updateId(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
updateId(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

state():FirmwareUpdateState {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : FirmwareUpdateState.Unknown;
}

receivedBytes():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt('0');
}

totalBytes():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt('0');
}

receivedChunks():number {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

totalChunks():number {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

progressPct():number {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : 0;
}

message():string|null
message(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
message(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 20);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

resultDetail():number {
  const offset = this.bb!.__offset(this.bb_pos, 22);
  return offset ? this.bb!.readInt32(this.bb_pos + offset) : 0;
}

static startFirmwareStatusReply(builder:flatbuffers.Builder) {
  builder.startObject(10);
}

static addResult(builder:flatbuffers.Builder, result:CommandResultCode) {
  builder.addFieldInt8(0, result, CommandResultCode.Accepted);
}

static addUpdateId(builder:flatbuffers.Builder, updateIdOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, updateIdOffset, 0);
}

static addState(builder:flatbuffers.Builder, state:FirmwareUpdateState) {
  builder.addFieldInt8(2, state, FirmwareUpdateState.Unknown);
}

static addReceivedBytes(builder:flatbuffers.Builder, receivedBytes:bigint) {
  builder.addFieldInt64(3, receivedBytes, BigInt('0'));
}

static addTotalBytes(builder:flatbuffers.Builder, totalBytes:bigint) {
  builder.addFieldInt64(4, totalBytes, BigInt('0'));
}

static addReceivedChunks(builder:flatbuffers.Builder, receivedChunks:number) {
  builder.addFieldInt32(5, receivedChunks, 0);
}

static addTotalChunks(builder:flatbuffers.Builder, totalChunks:number) {
  builder.addFieldInt32(6, totalChunks, 0);
}

static addProgressPct(builder:flatbuffers.Builder, progressPct:number) {
  builder.addFieldInt8(7, progressPct, 0);
}

static addMessage(builder:flatbuffers.Builder, messageOffset:flatbuffers.Offset) {
  builder.addFieldOffset(8, messageOffset, 0);
}

static addResultDetail(builder:flatbuffers.Builder, resultDetail:number) {
  builder.addFieldInt32(9, resultDetail, 0);
}

static endFirmwareStatusReply(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createFirmwareStatusReply(builder:flatbuffers.Builder, result:CommandResultCode, updateIdOffset:flatbuffers.Offset, state:FirmwareUpdateState, receivedBytes:bigint, totalBytes:bigint, receivedChunks:number, totalChunks:number, progressPct:number, messageOffset:flatbuffers.Offset, resultDetail:number):flatbuffers.Offset {
  FirmwareStatusReply.startFirmwareStatusReply(builder);
  FirmwareStatusReply.addResult(builder, result);
  FirmwareStatusReply.addUpdateId(builder, updateIdOffset);
  FirmwareStatusReply.addState(builder, state);
  FirmwareStatusReply.addReceivedBytes(builder, receivedBytes);
  FirmwareStatusReply.addTotalBytes(builder, totalBytes);
  FirmwareStatusReply.addReceivedChunks(builder, receivedChunks);
  FirmwareStatusReply.addTotalChunks(builder, totalChunks);
  FirmwareStatusReply.addProgressPct(builder, progressPct);
  FirmwareStatusReply.addMessage(builder, messageOffset);
  FirmwareStatusReply.addResultDetail(builder, resultDetail);
  return FirmwareStatusReply.endFirmwareStatusReply(builder);
}

unpack(): FirmwareStatusReplyT {
  return new FirmwareStatusReplyT(
    this.result(),
    this.updateId(),
    this.state(),
    this.receivedBytes(),
    this.totalBytes(),
    this.receivedChunks(),
    this.totalChunks(),
    this.progressPct(),
    this.message(),
    this.resultDetail()
  );
}


unpackTo(_o: FirmwareStatusReplyT): void {
  _o.result = this.result();
  _o.updateId = this.updateId();
  _o.state = this.state();
  _o.receivedBytes = this.receivedBytes();
  _o.totalBytes = this.totalBytes();
  _o.receivedChunks = this.receivedChunks();
  _o.totalChunks = this.totalChunks();
  _o.progressPct = this.progressPct();
  _o.message = this.message();
  _o.resultDetail = this.resultDetail();
}
}

export class FirmwareStatusReplyT {
constructor(
  public result: CommandResultCode = CommandResultCode.Accepted,
  public updateId: string|Uint8Array|null = null,
  public state: FirmwareUpdateState = FirmwareUpdateState.Unknown,
  public receivedBytes: bigint = BigInt('0'),
  public totalBytes: bigint = BigInt('0'),
  public receivedChunks: number = 0,
  public totalChunks: number = 0,
  public progressPct: number = 0,
  public message: string|Uint8Array|null = null,
  public resultDetail: number = 0
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const updateId = (this.updateId !== null ? builder.createString(this.updateId!) : 0);
  const message = (this.message !== null ? builder.createString(this.message!) : 0);

  return FirmwareStatusReply.createFirmwareStatusReply(builder,
    this.result,
    updateId,
    this.state,
    this.receivedBytes,
    this.totalBytes,
    this.receivedChunks,
    this.totalChunks,
    this.progressPct,
    message,
    this.resultDetail
  );
}
}
