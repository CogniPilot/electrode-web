// automatically generated by the FlatBuffers compiler, do not modify

import * as flatbuffers from 'flatbuffers';

import { FirmwareUpdateState } from '../../synapse/cmd/firmware-update-state';
import { CommandResultCode } from '../../synapse/types/command-result-code';


export class FirmwareInfoReply {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):FirmwareInfoReply {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsFirmwareInfoReply(bb:flatbuffers.ByteBuffer, obj?:FirmwareInfoReply):FirmwareInfoReply {
  return (obj || new FirmwareInfoReply()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsFirmwareInfoReply(bb:flatbuffers.ByteBuffer, obj?:FirmwareInfoReply):FirmwareInfoReply {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new FirmwareInfoReply()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

result():CommandResultCode {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : CommandResultCode.Accepted;
}

target():string|null
target(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
target(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

boardId():string|null
boardId(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
boardId(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

activeVersion():string|null
activeVersion(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
activeVersion(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

bootloader():string|null
bootloader(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
bootloader(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

state():FirmwareUpdateState {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : FirmwareUpdateState.Unknown;
}

maintenanceFlags():number {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.readUint16(this.bb_pos + offset) : 0;
}

maxImageSize():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt('0');
}

maxChunkSize():number {
  const offset = this.bb!.__offset(this.bb_pos, 20);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

message():string|null
message(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
message(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 22);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

resultDetail():number {
  const offset = this.bb!.__offset(this.bb_pos, 24);
  return offset ? this.bb!.readInt32(this.bb_pos + offset) : 0;
}

static startFirmwareInfoReply(builder:flatbuffers.Builder) {
  builder.startObject(11);
}

static addResult(builder:flatbuffers.Builder, result:CommandResultCode) {
  builder.addFieldInt8(0, result, CommandResultCode.Accepted);
}

static addTarget(builder:flatbuffers.Builder, targetOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, targetOffset, 0);
}

static addBoardId(builder:flatbuffers.Builder, boardIdOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, boardIdOffset, 0);
}

static addActiveVersion(builder:flatbuffers.Builder, activeVersionOffset:flatbuffers.Offset) {
  builder.addFieldOffset(3, activeVersionOffset, 0);
}

static addBootloader(builder:flatbuffers.Builder, bootloaderOffset:flatbuffers.Offset) {
  builder.addFieldOffset(4, bootloaderOffset, 0);
}

static addState(builder:flatbuffers.Builder, state:FirmwareUpdateState) {
  builder.addFieldInt8(5, state, FirmwareUpdateState.Unknown);
}

static addMaintenanceFlags(builder:flatbuffers.Builder, maintenanceFlags:number) {
  builder.addFieldInt16(6, maintenanceFlags, 0);
}

static addMaxImageSize(builder:flatbuffers.Builder, maxImageSize:bigint) {
  builder.addFieldInt64(7, maxImageSize, BigInt('0'));
}

static addMaxChunkSize(builder:flatbuffers.Builder, maxChunkSize:number) {
  builder.addFieldInt32(8, maxChunkSize, 0);
}

static addMessage(builder:flatbuffers.Builder, messageOffset:flatbuffers.Offset) {
  builder.addFieldOffset(9, messageOffset, 0);
}

static addResultDetail(builder:flatbuffers.Builder, resultDetail:number) {
  builder.addFieldInt32(10, resultDetail, 0);
}

static endFirmwareInfoReply(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createFirmwareInfoReply(builder:flatbuffers.Builder, result:CommandResultCode, targetOffset:flatbuffers.Offset, boardIdOffset:flatbuffers.Offset, activeVersionOffset:flatbuffers.Offset, bootloaderOffset:flatbuffers.Offset, state:FirmwareUpdateState, maintenanceFlags:number, maxImageSize:bigint, maxChunkSize:number, messageOffset:flatbuffers.Offset, resultDetail:number):flatbuffers.Offset {
  FirmwareInfoReply.startFirmwareInfoReply(builder);
  FirmwareInfoReply.addResult(builder, result);
  FirmwareInfoReply.addTarget(builder, targetOffset);
  FirmwareInfoReply.addBoardId(builder, boardIdOffset);
  FirmwareInfoReply.addActiveVersion(builder, activeVersionOffset);
  FirmwareInfoReply.addBootloader(builder, bootloaderOffset);
  FirmwareInfoReply.addState(builder, state);
  FirmwareInfoReply.addMaintenanceFlags(builder, maintenanceFlags);
  FirmwareInfoReply.addMaxImageSize(builder, maxImageSize);
  FirmwareInfoReply.addMaxChunkSize(builder, maxChunkSize);
  FirmwareInfoReply.addMessage(builder, messageOffset);
  FirmwareInfoReply.addResultDetail(builder, resultDetail);
  return FirmwareInfoReply.endFirmwareInfoReply(builder);
}

unpack(): FirmwareInfoReplyT {
  return new FirmwareInfoReplyT(
    this.result(),
    this.target(),
    this.boardId(),
    this.activeVersion(),
    this.bootloader(),
    this.state(),
    this.maintenanceFlags(),
    this.maxImageSize(),
    this.maxChunkSize(),
    this.message(),
    this.resultDetail()
  );
}


unpackTo(_o: FirmwareInfoReplyT): void {
  _o.result = this.result();
  _o.target = this.target();
  _o.boardId = this.boardId();
  _o.activeVersion = this.activeVersion();
  _o.bootloader = this.bootloader();
  _o.state = this.state();
  _o.maintenanceFlags = this.maintenanceFlags();
  _o.maxImageSize = this.maxImageSize();
  _o.maxChunkSize = this.maxChunkSize();
  _o.message = this.message();
  _o.resultDetail = this.resultDetail();
}
}

export class FirmwareInfoReplyT {
constructor(
  public result: CommandResultCode = CommandResultCode.Accepted,
  public target: string|Uint8Array|null = null,
  public boardId: string|Uint8Array|null = null,
  public activeVersion: string|Uint8Array|null = null,
  public bootloader: string|Uint8Array|null = null,
  public state: FirmwareUpdateState = FirmwareUpdateState.Unknown,
  public maintenanceFlags: number = 0,
  public maxImageSize: bigint = BigInt('0'),
  public maxChunkSize: number = 0,
  public message: string|Uint8Array|null = null,
  public resultDetail: number = 0
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const target = (this.target !== null ? builder.createString(this.target!) : 0);
  const boardId = (this.boardId !== null ? builder.createString(this.boardId!) : 0);
  const activeVersion = (this.activeVersion !== null ? builder.createString(this.activeVersion!) : 0);
  const bootloader = (this.bootloader !== null ? builder.createString(this.bootloader!) : 0);
  const message = (this.message !== null ? builder.createString(this.message!) : 0);

  return FirmwareInfoReply.createFirmwareInfoReply(builder,
    this.result,
    target,
    boardId,
    activeVersion,
    bootloader,
    this.state,
    this.maintenanceFlags,
    this.maxImageSize,
    this.maxChunkSize,
    message,
    this.resultDetail
  );
}
}
