// automatically generated by the FlatBuffers compiler, do not modify

import * as flatbuffers from 'flatbuffers';

import { FirmwareUpdateState } from '../../synapse/cmd/firmware-update-state';
import { CommandResultCode } from '../../synapse/types/command-result-code';


export class FirmwareProgress {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):FirmwareProgress {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsFirmwareProgress(bb:flatbuffers.ByteBuffer, obj?:FirmwareProgress):FirmwareProgress {
  return (obj || new FirmwareProgress()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsFirmwareProgress(bb:flatbuffers.ByteBuffer, obj?:FirmwareProgress):FirmwareProgress {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new FirmwareProgress()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

timestampUs():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt('0');
}

updateId():string|null
updateId(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
updateId(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

state():FirmwareUpdateState {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : FirmwareUpdateState.Unknown;
}

result():CommandResultCode {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : CommandResultCode.Accepted;
}

receivedBytes():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt('0');
}

totalBytes():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt('0');
}

receivedChunks():number {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

totalChunks():number {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

progressPct():number {
  const offset = this.bb!.__offset(this.bb_pos, 20);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : 0;
}

message():string|null
message(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
message(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 22);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

resultDetail():number {
  const offset = this.bb!.__offset(this.bb_pos, 24);
  return offset ? this.bb!.readInt32(this.bb_pos + offset) : 0;
}

static startFirmwareProgress(builder:flatbuffers.Builder) {
  builder.startObject(11);
}

static addTimestampUs(builder:flatbuffers.Builder, timestampUs:bigint) {
  builder.addFieldInt64(0, timestampUs, BigInt('0'));
}

static addUpdateId(builder:flatbuffers.Builder, updateIdOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, updateIdOffset, 0);
}

static addState(builder:flatbuffers.Builder, state:FirmwareUpdateState) {
  builder.addFieldInt8(2, state, FirmwareUpdateState.Unknown);
}

static addResult(builder:flatbuffers.Builder, result:CommandResultCode) {
  builder.addFieldInt8(3, result, CommandResultCode.Accepted);
}

static addReceivedBytes(builder:flatbuffers.Builder, receivedBytes:bigint) {
  builder.addFieldInt64(4, receivedBytes, BigInt('0'));
}

static addTotalBytes(builder:flatbuffers.Builder, totalBytes:bigint) {
  builder.addFieldInt64(5, totalBytes, BigInt('0'));
}

static addReceivedChunks(builder:flatbuffers.Builder, receivedChunks:number) {
  builder.addFieldInt32(6, receivedChunks, 0);
}

static addTotalChunks(builder:flatbuffers.Builder, totalChunks:number) {
  builder.addFieldInt32(7, totalChunks, 0);
}

static addProgressPct(builder:flatbuffers.Builder, progressPct:number) {
  builder.addFieldInt8(8, progressPct, 0);
}

static addMessage(builder:flatbuffers.Builder, messageOffset:flatbuffers.Offset) {
  builder.addFieldOffset(9, messageOffset, 0);
}

static addResultDetail(builder:flatbuffers.Builder, resultDetail:number) {
  builder.addFieldInt32(10, resultDetail, 0);
}

static endFirmwareProgress(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createFirmwareProgress(builder:flatbuffers.Builder, timestampUs:bigint, updateIdOffset:flatbuffers.Offset, state:FirmwareUpdateState, result:CommandResultCode, receivedBytes:bigint, totalBytes:bigint, receivedChunks:number, totalChunks:number, progressPct:number, messageOffset:flatbuffers.Offset, resultDetail:number):flatbuffers.Offset {
  FirmwareProgress.startFirmwareProgress(builder);
  FirmwareProgress.addTimestampUs(builder, timestampUs);
  FirmwareProgress.addUpdateId(builder, updateIdOffset);
  FirmwareProgress.addState(builder, state);
  FirmwareProgress.addResult(builder, result);
  FirmwareProgress.addReceivedBytes(builder, receivedBytes);
  FirmwareProgress.addTotalBytes(builder, totalBytes);
  FirmwareProgress.addReceivedChunks(builder, receivedChunks);
  FirmwareProgress.addTotalChunks(builder, totalChunks);
  FirmwareProgress.addProgressPct(builder, progressPct);
  FirmwareProgress.addMessage(builder, messageOffset);
  FirmwareProgress.addResultDetail(builder, resultDetail);
  return FirmwareProgress.endFirmwareProgress(builder);
}

unpack(): FirmwareProgressT {
  return new FirmwareProgressT(
    this.timestampUs(),
    this.updateId(),
    this.state(),
    this.result(),
    this.receivedBytes(),
    this.totalBytes(),
    this.receivedChunks(),
    this.totalChunks(),
    this.progressPct(),
    this.message(),
    this.resultDetail()
  );
}


unpackTo(_o: FirmwareProgressT): void {
  _o.timestampUs = this.timestampUs();
  _o.updateId = this.updateId();
  _o.state = this.state();
  _o.result = this.result();
  _o.receivedBytes = this.receivedBytes();
  _o.totalBytes = this.totalBytes();
  _o.receivedChunks = this.receivedChunks();
  _o.totalChunks = this.totalChunks();
  _o.progressPct = this.progressPct();
  _o.message = this.message();
  _o.resultDetail = this.resultDetail();
}
}

export class FirmwareProgressT {
constructor(
  public timestampUs: bigint = BigInt('0'),
  public updateId: string|Uint8Array|null = null,
  public state: FirmwareUpdateState = FirmwareUpdateState.Unknown,
  public result: CommandResultCode = CommandResultCode.Accepted,
  public receivedBytes: bigint = BigInt('0'),
  public totalBytes: bigint = BigInt('0'),
  public receivedChunks: number = 0,
  public totalChunks: number = 0,
  public progressPct: number = 0,
  public message: string|Uint8Array|null = null,
  public resultDetail: number = 0
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const updateId = (this.updateId !== null ? builder.createString(this.updateId!) : 0);
  const message = (this.message !== null ? builder.createString(this.message!) : 0);

  return FirmwareProgress.createFirmwareProgress(builder,
    this.timestampUs,
    updateId,
    this.state,
    this.result,
    this.receivedBytes,
    this.totalBytes,
    this.receivedChunks,
    this.totalChunks,
    this.progressPct,
    message,
    this.resultDetail
  );
}
}
